datasource db {
  provider = "sqlite"
}

generator client {
  provider = "prisma-client-js"
}

model Report {
  id        String   @id @default(cuid())
  decision  String
  risk      Float
  score     Float
  createdAt DateTime @default(now())
}
